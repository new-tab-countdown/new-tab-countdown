import DateOption from '../common/DateOption';

export const TODAY = new DateOption('today', 'day');

export const THIS_WEEK = new DateOption('this week', 'week');

export const THIS_MONTH = new DateOption('this month', 'month');

export const THIS_YEAR = new DateOption('this year', 'year');
